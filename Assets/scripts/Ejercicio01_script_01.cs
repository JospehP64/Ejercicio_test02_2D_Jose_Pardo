using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio01_script_01 : MonoBehaviour
{
    //declarar variable y asignar un valor a la variable (clase nombre valor | ejemplo: int edad = 14;)
    int vida = 7;//numeros enteros
    string Nombre;//por defecto es ""
    float Porcentaje_Acierto;//numeros decimales
    bool quemado; //verdadero o falso
    char solucion_puzzle; //por defecto es ''

    // Start is called before the first frame update
    void Start()
    {
        int vida;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
